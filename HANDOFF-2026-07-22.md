# AIMA Handoff — 2026-07-22

**From:** Claude Code session (OAuth-hardening task)
**For:** the next session (any operator/agent picking this up)
**One-line status:** Pipeline code is fixed and #29 is unblocked, but the machine this
runs on is compromised — **security cleanup and credential rotation come before any
pipeline run.**

---

## 🔴 IR UPDATE — 2026-07-22 (second session): NEW findings + more containment

A follow-up investigation re-verified the prior containment (held) and **found two things
the first pass missed**, both now handled:

1. **Second-stage RAT recovered from `%TEMP%\5e9292cd.js` (265 KB, dated 2026-07-02).**
   The Deno loader is a *downloader*: it pulls a payload to `%TEMP%\<huid>.js` and runs it
   with `deno -A`. That second stage was still sitting in the auto-execute drop path.
   Statically characterized (same obfuscator.io family as the loader) — imports
   `WebSocket` + `child`(process) + `fetch` + `atob`/`require` → a full remote-access
   backdoor bundle. **Moved to quarantine** (not deleted):
   `Quarantine_20260722\TEMP-secondstage-5e9292cd-20260702.js`.
   - **SHA256:** `4AEA98FA8C26843410A5B7CA2518C31D7A5FC725137AB95A448DAA134FD577A5`
   - Proves the loader was *live and successfully beaconing* as recently as **2026-07-02**.
2. **The loader's runtime was the malware's own: Scoop-installed `deno` dated 2026-04-17**
   (`scoop\apps\deno\2.7.12` + `scoop\shims\deno.exe`, both timestamped 09:02–09:04 on
   2026-04-17 — the *same morning* as the loader drop; deno is the ONLY shim in Scoop).
   This is not a pre-existing dev tool — the malware installed its own interpreter.
   **REMOVED this session** (`scoop uninstall deno`, operator-approved). Verified: no
   `deno.exe` remains anywhere on disk (searched user profile + Program Files + ProgramData,
   depth 6). The loader now has no runtime = fully neutered even if the loader file returns.

3. **🔴 THIRD payload found — was hidden by a Defender exclusion (the big one).** An
   elevated `Get-MpPreference` (operator ran it) revealed malicious exclusions:
   `ExclusionPath = C:\ProgramData\CBsjecXC3i` (+ its `pythonw.exe`) and
   `ExclusionProcess = pythonw.exe`. That folder is a **second PyArmor bundle**, sibling of
   CoffeeFolder — bundled Python 3.9 + `pyarmor_runtime_000000` + a decoy `Coffee.mp4`
   (verified genuine MP4, `ftypisom`). PyArmor **9.1.9 (trial), 2026-02-14** — i.e. ONE DAY
   OLDER than CoffeeFolder (9.2.1, 2026-02-15), so **CBsjecXC3i was the original drop,
   CoffeeFolder the next-day update.** The first session removed the newer sibling but never
   saw this one *because it was Defender-excluded*. No task/Run-key/process currently launches
   it (launcher was the already-removed `pythonw.exe` task) — it was dormant on disk.
   **Handled:** moved to `Quarantine_20260722\CBsjecXC3i` (operator's elevated shell), then
   removed all three malicious exclusions; verified `ExclusionPath`/`ExclusionProcess` now
   empty. Re-swept for other PyArmor bundles / stray bundled `pythonw.exe` across ProgramData
   + AppData + Public + user profile — **none** beyond the two now in quarantine.
   - **SHA256** `C8B46A344A676B1B24029109886066B05703307D96A4256FBD73ED2567D14202` Coffee.mp4
   - **SHA256** `593E1FDD8AEF020F5A27776C706AA733AA9ED5059AFFDA12AEAE6DDFF1002C6D` pyarmor_runtime.pyd
   - **Lesson for future sweeps:** ALWAYS check `Get-MpPreference` exclusions (needs admin)
     BEFORE trusting a filesystem sweep — a Defender-excluded folder is invisible to on-access
     detection and easy to miss by name.

**Also done this session:** flushed the DNS cache (all 4 C2 domains cleared; they were
*aging out*, TTLs counting down — NOT being re-resolved, so no active beaconing at scan
time). Re-swept Run/RunOnce keys, startup folders, WMI subscriptions, Winlogon Shell/
Userinit, hosts file, services (user-writable paths), local accounts/admins, scheduled-task
actions — **all clean**. No malware processes, no live C2 connections.

**Corrected malware timeline:** 2026-02-15 CoffeeFolder+`pythonw.exe` task (persistence #1)
→ 2026-04-17 Scoop deno + loader `5e9292cd.js` + Run key (persistence #2) → 2026-07-02
loader pulled the 265 KB RAT second stage.

**Could NOT do (needs an ELEVATED shell — this session was non-admin):** view Defender
exclusions, launch a Defender **offline** scan. Run those from an admin PowerShell. The
credential-rotation + OS-reinstall guidance below still stands unchanged.

---

> **UPDATE (2026-07-22, end of session):** Machine remediated (all malware removed + quarantined,
> SFC clean, Defender hardened), all Tier-1/2 credentials rotated (GitHub, Google, Apple ID @me.com;
> Anthropic/Pexels/GA4/Higgsfield/LinkedIn), and **`claude` CLI OAuth re-authed — verified live
> (`claude --print` returns normally).** The Scout/Trend-Scout halt blocker is CLEARED. Remaining
> before `python run.py`: put the freshly-rotated secrets back into `agents/.env`,
> `linkedin_pipeline/.env` + `tokens.json`, and the new GA4 service-account JSON (old ones were
> revoked); next `git push` will prompt for the new GitHub token (cached cred was cleared).
>
> **Full incident report:** `C:\Users\ShadowMonkey\Quarantine_20260722\INCIDENT-REPORT.md`
> (all 3 payloads + Deno loader + second-stage RAT, C2 IOCs, SHA256 hashes, timeline,
> credential-rotation checklist). Lives with the quarantined samples.

## ⚠️ READ THIS FIRST — active machine compromise (unrelated to the pipeline bug)

While investigating the pipeline crashes I found malware that had been running as the
`ShadowMonkey` user for ~5 months. It has been **contained** this session, but nothing
that was already stolen can be un-stolen. Treat this as an incident, not a footnote.

### What it was
- **`C:\ProgramData\CoffeeFolder\`** — a PyArmor 9.2.1 (trial)-obfuscated Python payload
  (`__init__.py`) with its own bundled Python 3.9 runtime, auto-started at every logon by a
  scheduled task literally named **`pythonw.exe`** (windowless). Payload dated 2026-02-15.
- **`…\AppData\Roaming\5e9292cd.js`** — a Deno script launched via an HKCU Run key named
  `5e9292cd`, run as `conhost --headless … deno.exe -A …` (**`-A` = ALL permissions**:
  full FS, network, env). Dated 2026-04-17. Obfuscated; reconstructs a C2 URL from string
  fragments (`'http'+'://l'+…+'beri'…` → `lavrentiyberia.com`).
- **C2 domains** seen in the DNS cache (Soviet-figure naming theme, bulletproof-VPS IPs):
  `lavrentiyberia.com` (79.132.131.95), `admiralkolchak.com` (185.219.221.191),
  `annaionovna.com` (199.91.220.216), `popopopopi.com` (23.94.252.191).
  (Note: the `96.6.236.246:443` connection is Akamai CDN — benign, ignore it.)

### What was done this session (containment only)
- Killed PIDs 708, 7720 (`pythonw.exe`), 10876 (`deno.exe`), 10820 (its conhost).
- Unregistered scheduled task `pythonw.exe` (was only disabled first, then fully removed).
- Deleted HKCU Run key `5e9292cd`.
- Moved both payloads to **`C:\Users\ShadowMonkey\Quarantine_20260722\`** (kept intact,
  NOT deleted, for analysis/VirusTotal) + saved the removed Run-key value there.
- Swept for further persistence (other tasks, hex-named Roaming scripts, WMI consumers) —
  **clean**; all remaining autoruns are legit (OneDrive, Opera, Zoom, Docker, Defender, etc.).
- Verified: no processes remain, no live connections to the C2 IPs.

### SHA256 (search these on VirusTotal — no upload needed)
```
8C4C38CA98BF1E2DBD2B41B227B464F6E08B09E55139286764B9EE6BFB74DCA4  CoffeeFolder\__init__.py
735C71C8B170D20828C61B683767E84795C63735B3AAE24BBC3F06C9DEFEC009  pyarmor_runtime.pyd
1F6992488102711FA8FAFAE9219DB9DF00834151EE196A395AE1E7ABC977722D  5e9292cd.js
```

### What still MUST happen (not yet done — needs Joe / a clean device)
1. **Rotate every credential this account could read, from a DIFFERENT clean machine:**
   GitHub token, LinkedIn tokens (`linkedin_pipeline/.env`), `agents/.env` keys
   (Anthropic / OpenRouter), GA4 service-account JSON, email, anything financial.
   Rotating from the infected box would just leak the new secrets.
2. **Microsoft Defender OFFLINE scan**, then strongly consider a **clean OS reinstall**
   (two independent persistence mechanisms + months of dwell time = reinstall is the only
   thing that's actually certain).
3. **Do NOT re-authenticate `claude` on this machine until it's clean** — see below.

---

## ✅ The actual task this session was for — DONE (commit `408bd94`)

**Problem:** the pipeline crashed 5× on 2026-07-22, all at `priya → trend_scout →
call_cc_agent → RuntimeError: CC OAuth expired`, because calendar row #29 was still
`TBD — Trending Topic` and every run called `trend_scout` (which needs the `claude` CLI)
to resolve it.

**Fixed:**
- **#29 unblocked** — row now reads *"The Lab That Runs Itself: How Autonomous Labs Are
  Compressing Materials Discovery From Years to Days"* (Kenji Nakamoto, AI Science).
  Topic surveyed via WebSearch, persisted through the sanctioned
  `trend_scout.persist_topic_to_calendar()` (Trend Scout stays sole calendar mutator).
  Two higher-ranked candidates rejected on dedup (humanoids → #20/#23; BCI → #33).
  Trace: `articles/research/the-lab-that-runs-itself-topic-selection.json`
  (honestly marked `resolved_by: claude_code_manual`). Cost $0.00.
- **Live-research carve-out** (`agents/base.py`) — `scout` and `trend_scout` now raise
  `LiveResearchUnavailableError` on auth failure **instead of** falling through to the
  tool-less direct-Anthropic (Tier B) fallback, even when `ANTHROPIC_API_KEY` is set. Tier B
  has no web search, so they would fabricate "trending" topics/stats from training data —
  a zero-hallucination-rule violation worse than an honest crash. Tier A (OpenRouter
  `:online`) stays allowed for them. `iris`/`cora`/`lumen`/writers keep Tier B unchanged.
- **Clean halt** (`agents/marco.py`, `run.py`) — Marco catches that error above Stage 9,
  returns `trend_scout_unavailable` (new `_classify()` outcome), writes a short
  traceback-free `### Pipeline HALT (recoverable)` block. **No state advance, no calendar
  mutation, nothing published.**
- **CLAUDE.md dedup** — both failure writers now go through `_append_or_bump_claude_md()`
  (keyed on `date|stage|error-first-line`); same-day repeats bump an `Occurrences:` counter
  instead of appending. Historical duplicates retro-compacted (24 → 18 CRASH blocks).
- **Task Scheduler theory killed** — the "running as SYSTEM" guess baked into `base.py`'s
  error text was FALSE: there is no scheduled task running this pipeline at all. The 5 runs
  were manual retries. Error text corrected.

**Verified live:** `python run.py` now builds the #29 spec, clears Priya + Trend Scout,
then halts cleanly at Scout (`outcome=trend_scout_unavailable`, cost $0.00) instead of
crashing.

---

## 🔜 To actually POST article #29 (the one remaining blocker)

The pipeline is **one human step** from working: the `claude` CLI's OAuth is expired
(`claude --print "ping"` → "OAuth session expired and could not be refreshed"). Fix:

1. **(after the security cleanup / on a clean machine)** open a terminal, run `claude`,
   complete the OAuth login.
2. `python run.py` — #29 is pre-resolved, so it goes straight through Trend Scout, Scout,
   Writer, Quill, Maya, Vera (QC_GATE=`auto`), Porter (publish), Nova (LinkedIn).

Until that re-auth, every run will keep halting cleanly at Scout — by design, not a bug.

---

## Git state
- Commit `408bd94` on `main` (NOT pushed — Porter pushes `main` itself, so the checkout was
  left on `main` rather than a side branch). Scoped to: `agents/base.py`, `agents/marco.py`,
  `run.py`, `CLAUDE.md`, `articles/aima-editorial-calendar.md`,
  `articles/research/the-lab-that-runs-itself-topic-selection.json`,
  `optimization/optimization_report.json`.
- Plenty of OTHER uncommitted WIP predates this session (see `git status`) — left untouched.

## Open items (see TASKS.md for the full list)
- Fund/decide OpenRouter (now the only tool-capable fallback for scout/trend_scout when CC
  OAuth is down).
- LinkedIn token refresh (`r_member_social`) — will need redoing after credential rotation.
- Longstanding cost/budget items (Scout over-budget, Cora over-budget, Priya cost).
